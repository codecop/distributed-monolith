position:
  x: 1
  y: 2

micronaut:
  application:
    name: distributedGolCell
  server:
    port: 80

  jms:
    activemq:
      classic:
        enabled: true
        connection-string: tcp://localhost:61616

config.jms:
  seedQueue: seed-messages
  aliveQueue: alive-messages
  tickQueue: tick-messages
  
netty:
  default:
    allocator:
      max-order: 3
